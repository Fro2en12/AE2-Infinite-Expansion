# AE2 Addon — Infinite Expansion v1.2.3

## [Unreleased] 上游 v1.3.0 同步（1.21.1 移植）

把上游 `upstream/main`（含 v1.3.0）自 v1.2.2 之后的功能同步进本分支；下方只列本轮新增与改动，既有功能（ME接口方块、配置存储卡等）不重复列出。以下为源码级改动，尚未发布构建（版本号待发布时再定）。

### 新增

- 🧩 **无限级装配处理器（核心）**：新方块贴入集成 CPU 簇后即可把合成样板列入「虚拟结算」白名单（样板槽 5×9×200 = 9000 格，带分页界面）；命中的样板材料直接消耗、产物瞬时注入，巨型合成不必再排队等物理装配。
- 📡 **ME接口（无限级）线缆面板**：供料站现在能装到 AE2 线缆上，机器贴线缆即可供料、不额外占格子；与方块版共用界面和全部参数，两者可无序合成 1:1 互转。
- ⇦ **退回网络按钮**：接口界面新增退网按钮，把蓄水池里未喂出的推送料与待入网缓存一次性插回网络——样板发错或任务放弃后，材料不用手动挖回来。
- 💧 **待入网缓存**：网络满或拒收时，抽到的产物与多余推送料先留在蓄水池并标记待补送，网络一有空间自动补送；这段时间它们不参与喂机器，也不会被当成回流材料。
- 🔌 **感应卡供电可配**：新增 `feederPowerFeCap`（单轮供电上限，默认 1 亿 FE）与 `feederPowerPassesPerTick`（每 tick 供电轮数）；速度卡 1 张把单轮上限 ×16，2 张则每轮灌满机器缺口（无上限）。
- 🖥 **集成 CPU 显示值可配置**：`cpuDisplayBytes` / `cpuDisplayThreads` 决定合成终端、合成确认界面与 CPU 列表里显示的数字；`cpuStorageText` / `cpuThreadsText` 非空时直接覆盖显示文本。只影响显示，真实容量与并行不受影响。
- 🎛 **CPU 界面面板可拖动**：集成 CPU 界面的量子分裂线程面板与巨型订单面板可自由拖动，位置写入 `config/ae2addon-cpu-ui.json`，重开界面后保持。
- ⚖️ **共享派发预算**：新增 `dispatchBudgetPerTick`（默认 20000，`0` = 不限），限制全网格每 tick 的成功派发次数，多个巨型订单同 tick 时不再把服务端 tick 拖垮。

### 修复

- 🧬 **增殖配方（自指 a+b=2a）卡死**：模板复制类配方补上种子起手判定与强制逐次结算，巨型增殖样板不再停在第一轮不动。
- 🧬 **装配模块增殖结算漏乘倍率**：装配处理器的即时结算在增殖样板下备料未乘批量倍率，是卡死的直接根因，已修复。
- 🔁 **增殖产物回流**：自指任务的每轮产物回流 crafting storage 充当下一轮种子，配合库存感知批量实现指数滚雪球（此前锁死在 N=1 线性推进）。
- ⏱ **虚拟结算期抢先接管**：模拟阶段命中增殖配方时直接接管并替换原合成计划，并去掉 `Integer.MAX` 截断。
- 🛟 **CPU 取消失败回退加固**：取消合成时若材料回退失败，不再丢料。

### 移植适配

- 📁 **数据目录单数化**：`data/ae2addon/recipes/` → `recipe/`、`loot_tables/` → `loot_table/`。1.21.1 只认单数目录——此前配方与方块战利品表完全不加载（合成不出、挖了不掉），现已恢复。
- 📦 **配方 result 格式**：`result: {"item": ...}` → `result: {"id": ..., "count": ...}`（1.21.1 数据包格式）。
- 🧾 **战利品表函数**：`copy_nbt` → `copy_components`（1.21.1 组件体系），集成 CPU 与装配处理器的掉落得以保留方块实体数据。
- 🔌 **线缆 part 按 AE2 19 Part 体系改写**：`IPart#onPartActivate` 等回调已移除，交互改走 `onUseItemOn`；NBT 读写带 `HolderLookup.Provider`；Forge 的 `getCapability` / `LazyOptional` 路径在 NeoForge 1.21 不可用。
- 🧱 **API 形态对齐**：`IPatternDetails#getOutputs()` 返回 `List`（AE2 19 不再是数组）、样板种类判定改用 1.21.1 类名、`ItemStack` 存取改用带 registries 的 `save` / `parseOptional`。
- ⚗️ **Mekanism 化学品适配**：Mekanism 10.7.x 统一为 `ChemicalStack` + `IChemicalHandler`，气体兼容拆成门面类与化学品实操类，引用可选依赖的类只在模组存在时加载，避免类加载验证期崩溃。

---

## 更新

- 🎨 **全新模组图标**：以 AE2 官方存储元件为基底 + 无限符号徽记（银色元件盘 + 蓝色标签带 + 无限环）
- ✨ 适配既有全部功能（无功能变更，纯视觉版本）

## 功能回顾

- **万能无限存储元件**：无限存储 / 自定义无限 / 全类型无限三种模式，拼音搜索中文配置界面
- **无限合成存储器**：单方块提供 Long.MAX_VALUE 字节合成存储，GridMixin 让 AE2 正确识别子类合成方块
- **无限并行处理单元**：单方块 Integer.MAX_VALUE 线程，移除原版 16 线程上限，万亿配方秒级完成

## 依赖信息

- Minecraft 1.21.1 / NeoForge 21.1.x
- 依赖：AE2（Applied Energistics 2）